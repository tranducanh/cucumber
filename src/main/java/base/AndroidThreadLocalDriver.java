package base;

import base.configuration.AndroidConfiguration;
import base.configuration.BaseConfiguration;

public class AndroidThreadLocalDriver extends BaseThreadLocalDriver {


    public AndroidThreadLocalDriver(AndroidConfiguration androidConfiguration) {
        super(androidConfiguration);
    }
}
